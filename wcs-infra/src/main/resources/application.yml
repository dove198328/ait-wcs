mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: cn.aitplus.wcs.core.domain.model
  configuration:
    map-underscore-to-camel-case: true

pagehelper:
  auto-dialect: true
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
